<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.hslu.fs19.mobpro.sweetbeeromine">
    <!-- Features -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" /> <!-- Permissions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!-- Acitivites -->

        <activity
            android:name=".CopyrightActivity"
            android:label="@string/copyright"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".TrophiesActivity"
            android:label="@string/trophies"
            android:screenOrientation="portrait" />
        <activity
            android:name=".AddBeerActivity"
            android:label="@string/add_beer"
            android:screenOrientation="portrait" />
        <activity
            android:name=".EditBeerActivity"
            android:label="@string/edit_beer"
            android:screenOrientation="portrait" />
        <activity
            android:name=".DetailBeerActivity"
            android:label="@string/details_beer"
            android:screenOrientation="portrait" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>